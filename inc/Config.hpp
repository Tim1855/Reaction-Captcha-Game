#ifndef CONFIG_HPP
#define CONFIG_HPP

#include <string> // Einbindung der Standardbibliothek für Strings

// Globale externe Variablen für die Pfadangaben
extern std::string imageFolder; // Pfad zum Ordner der Bilder
extern std::string bboxFolderPath; // Pfad zum Ordner der Bounding Boxes

#endif // CONFIG_HPP
