Klassen und Beziehungen
Menu

Attribute:
keine
Methoden:
void zeigeBegruessung()
void frageSpielerDaten(std::string &spielerName, int &anzahlBilder, int &sequenz, int &spielModus)
bool bestaetigeStart()
Spiel

Attribute:
std::string spielerName
int anzahlBilder
int sequenz
int spielModus
std::vector<Bild> bilder
std::vector<double> reaktionszeiten
Methoden:
Spiel(const std::string &name, int anzahlBilder, int sequenz, int modus)
void starten()
void zeigeErgebnisse()
void ladeBilder()
void directClickReaction(Bild &bild)
void colorChangeReaction(Bild &bild)
Bild

Attribute:
std::string dateiPfad
std::vector<std::vector<int>> boundingBoxen
Methoden:
Bild(const std::string &dateiPfad)
void anzeigen()
void zeichneBoundingBoxen()
bool klickErkannt(int x, int y)
Beziehungen
Menu fragt Spielerdaten ab und startet ein Spiel.
Spiel verwendet Bild-Objekte, um das Spiel darzustellen.